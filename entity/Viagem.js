class Viagem {
    constructor(viagem, data, status, pessoaId) {
      this.viagem = viagem;
      this.data = data;
      this.status = status;
      this.pessoaId =pessoaId;
    }
}

module.exports = Viagem;